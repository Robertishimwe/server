name: CI
env:
<<<<<<< HEAD
  DB_CONNECT: mongodb+srv://robertishimwe:kigali123@cluster0.rhbhy.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
  TOKEN_SECRET: DGUIHUIHGIGDURHROBERTDIURDUIRUIHIDUHRIUH
=======
  DB_CONNECT: ${{ secrets.DB_CONNECT }}
  TOKEN_SECRET: ${{ secrets.TOKEN_SECRET }}
  CI: true
>>>>>>> fb9ad9dcc997ef8c5a3bf6071062d4e47c940847

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build
      - run: npm test
      - name: Code Climate Test Reporter
        uses: aktions/codeclimate-test-reporter@v1
        with:
          codeclimate-test-reporter-id: 22051e07c57c969f74159166e072e506d1aa5df8dcc83e1f53834ac302720b6a
          command: after-build
    
    
